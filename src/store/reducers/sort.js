const initial = null
const SHORT = 'SHORT'

const sort = (state = initial, action) => {
  switch (action.type) {
    case SHORT:
      return state
    default:
      return state
  }
}

export default sort
export {
  SHORT
}
